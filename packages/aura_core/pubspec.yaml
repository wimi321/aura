name: aura_core
version: 0.1.0
publish_to: 'none'
description: Core backend engine and orchestration layer for the Aura offline roleplay app.

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  archive: ^4.0.7
  collection: ^1.19.1
  crypto: ^3.0.6
  meta: ^1.16.0
  path: ^1.9.1

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.15
